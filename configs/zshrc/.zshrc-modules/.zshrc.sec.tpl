#!/bin/zsh
# Secrets injected from 1Password via `op inject`
# Template: .zshrc.sec.tpl -> .zshrc.sec (gitignored)
#
# Find names with: op vault list  /  op item list --vault Personal

#export TEST_ANTHROPIC_API_KEY="{{ op://Personal/Anthropic/credential }}"
