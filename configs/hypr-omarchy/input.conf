# Control your input devices
# See https://wiki.hypr.land/Configuring/Variables/#input
input {
  # Use multiple keyboard layouts and switch between them with Left Alt + Right Alt
  # kb_layout = us,dk,eu
  kb_layout = us
  kb_options = compose:caps # ,grp:alts_toggle

  # Change speed of keyboard repeat
  repeat_rate = 40
  repeat_delay = 600

  # Start with numlock on by default
  numlock_by_default = true

  # Increase sensitivity for mouse/trackpad (default: 0)
  # sensitivity = 0.35

  touchpad {
    # Non-natural scrolling. Set explicitly rather than left to omarchy's
    # default/hypr/input.conf so an omarchy update can't flip it.
    natural_scroll = false

    # Use two-finger clicks for right-click instead of lower-right corner
    # Kills the bottom-right software button zone that a resting palm/thumb
    # was triggering as a right-click on this clickpad (PROP=BUTTONPAD).
    clickfinger_behavior = true

    # Control the speed of your scrolling
    scroll_factor = 0.4

    # Left-click-and-drag with three fingers (macOS "three finger drag")
    drag_3fg = 1
  }
}

# Scroll nicely in the terminal
windowrule = match:class (Alacritty|kitty|foot), scroll_touchpad 1.5
windowrule = match:class com.mitchellh.ghostty, scroll_touchpad 0.2

# Swipe between workspaces, macOS "switch Spaces" style.
# See https://wiki.hyprland.org/Configuring/Gestures/
# FOUR fingers, not three: drag_3fg above claims the 3-finger gesture, and a
# 3-finger swipe here would fight it. macOS uses 4 fingers for the same reason.
gesture = 4, horizontal, workspace
