name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  health-check:
    name: Health check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run repository health check
        run: bash script/dev/check.sh
