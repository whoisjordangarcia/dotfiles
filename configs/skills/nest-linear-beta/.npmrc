# Block lifecycle scripts from dependencies (supply-chain security).
# If a dependency genuinely needs build scripts, allowlist via
# trustedDependencies in package.json or run a one-shot `npm rebuild <pkg>`.
ignore-scripts=true

# Pin registry to prevent environment-level overrides.
registry=https://registry.npmjs.org/
