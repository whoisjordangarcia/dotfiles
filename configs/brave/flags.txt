# brave://flags experiments to enable — one per line, e.g. https-by-default@2.
# SOFT + ADDITIVE: unioned into Local State's enabled_labs_experiments (never
# disables flags you enabled yourself); turn any off at brave://flags anytime.
# GENERATED by brave-sync. Applied while Brave is CLOSED (run: brave-theme).
https-by-default@2
